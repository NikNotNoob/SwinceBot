const fs = require("fs");
const path = require("path");

function readFile(file) {
	//Opens file for reading, handles errors, returns promises
}

function writeFile(file, data) {
	//Opens file for writing, handles errors, creates file if not exists, returns promises
}

module.exports = { readFile, writeFile };
