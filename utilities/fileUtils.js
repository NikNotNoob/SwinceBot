const fs = require("fs");
const path = require("path");

function readFile(file) {
	//TODO Opens file for reading, handles errors, returns promises
}

function writeFile(file, data) {
	//TODO Opens file for writing, handles errors, creates file if not exists, returns promises
}

module.exports = { readFile, writeFile };
